# browser-qc · Glamour orb.html idle-wander drift recheck · rollup: PASS

Recheck of the idle "wander" jitter/teleport bug (fixed via segment-target memoization).
Served from `/private/tmp/glam-v01-proof` (proof dir, not the repo checkout — `orb.html` regenerated 2026-07-22 16:29, tested 16:33, ~4 min fresh). Hard-navigated with cache-busting query param.

## Page load / bundle parse — PASS
- Tested:   navigate to `http://localhost:8144/orb.html?_qc=<ts>`, check `typeof window.P` and console for SyntaxError.
- Expected: `P` is an object, no console errors.
- Actual:   `typeof window.P === "object"`; `window.P.__byId['face']` present; console: 0 errors, 0 warnings.
- Evidence: console check inline (Total messages: 0 (Errors: 0, Warnings: 0)).

## Idle wander smoothness (numeric) — PASS
- Tested:   sampled `{x,y}` of `P.__byId['face']` every ~50ms for ~2s (40 samples) via `javascript_tool`/`eval` Promise+setInterval.
- Expected: max consecutive-sample jump well under ~8px (ease over stepMs≈900, ellipse rx26/ry16); FAIL threshold ≥8px or frozen.
- Actual:   n=40 samples, **max consecutive jump = 3.18px** (t=560→610ms), **total path length = 43.28px** over 2s (not frozen — comfortably moving). No teleports observed anywhere in the series.
- Sample rows (t ms, x, y):
  - t=60:   x=166.861, y=176.670
  - t=110:  x=166.664, y=176.553
  - t=560:  x=177.864, y=186.652
  - t=610:  x=180.225, y=188.781  (largest step, 3.18px)
  - t=1958: x=201.394, y=198.638
  - t=2009: x=201.316, y=198.345
- Evidence: raw sample array + python jump/path computation (session-local, not persisted as a file per instructions — reproducible via the eval snippet above).

## State-switching (Check) — PASS
- Tested:   click "Check" button, then `window.P.getState()`.
- Expected: state becomes `'checked'`, orb renders green with checkmark glyph.
- Actual:   `getState()` returned `"checked"`; orb visibly green circle with white checkmark.
- Evidence: /Users/khavu/Project/docs/superpowers/qc/evidence/v0.1/orb-checked.png

## Idle visual baseline — PASS
- Tested:   initial render before any interaction.
- Expected: blue circle, neutral face (two eye bars + mouth bar).
- Actual:   matches — blue orb, neutral face.
- Evidence: /Users/khavu/Project/docs/superpowers/qc/evidence/v0.1/orb-idle.png

## Server / cwd note
- Server on :8144 (PID 23124, python) serves from `/private/tmp/glam-v01-proof`, not the `glamour` repo checkout. This is the proof-bundle directory the task described ("page was just regenerated with the fixed player bundle") — flagging per convention since it isn't the repo's own dev-server cwd, but the file (`orb.html`, 703603 bytes, mtime 16:29) is fresh relative to test time (16:33) and matches the described fix scenario.

---
**Rollup: PASS.** Bundle parses cleanly (no SyntaxError, `window.P` live). Idle wander is smooth (max 3.18px/50ms step, no teleports) — jitter/teleport bug confirmed fixed. State-switching (Check → checked, green/checkmark) still works.
